{% set name = "ipython" %}

package:
  name: "{{ name|lower }}"
  # didn't work
  # version: {{ GIT_DESCRIBE_TAG }}
  version: "7.11.0"


# Read more about the format of this file at
# https://conda.io/projects/conda-build/en/latest/resources/define-metadata.html#

source:
  path: ../

build:
  number: {{ GIT_DESCRIBE_NUMBER }}
  # Note that this will override the default build string with the Python
  # and NumPy versions
  #     string: {{ GIT_BUILD_STR }}
  #     Yeah don't uncomment the line below
  # script: {{ PYTHON }} "-m pip install" {{ PWD }} " --no-deps --ignore-installed -vv "
  # This would replace the autogenerated build.sh / build.bat they usually dump
  skip: True  # [py2k]
  entry_points:
    - ipython = IPython:start_ipython
    - ipython3 = IPython:start_ipython
    - iptest = IPython.testing.iptestcontroller:main
    - iptest3 = IPython.testing.iptestcontroller:main

requirements:
  host:
      # - pip I mean i suppose conda could also work here
    - python
  run:
    - appnope  # [osx]
    - backcall
    - colorama  # [win]
    - decorator
    - jedi >=0.10
    - pexpect  # [unix]
    - pickleshare
    - prompt_toolkit >=2.0.0,<3
    - pygments
    - python  # [py>3.6]
    - traitlets >=4.2
    - win_unicode_console >=0.5  # [win and py<36]
 # could add a section here for
 # build:
   # - wheel
   # then outputs: name: - IPython type: wheel

test:
  requires:
    - coverage
    - curio  # [unix]
    - ipykernel  # [not (aarch64 or ppc64le)]
    - matplotlib  # [not (aarch64 or ppc64le)]
    - nbformat
    - nose >=0.10.1
    - pexpect  # [unix]
    - pygments
    - pytest
    - requests
    - sphinx
    - testpath
    - trio   # [not (aarch64 or ppc64le)]

  imports:
    - IPython

  commands:
    - ipython -h
    - ipython3 -h
    # [unix and not (aarch64 or ppc64le)]
    # - NOSE_EXCLUDE="test_history|recursion" iptest

about:
  home: https://ipython.org/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: "IPython: Productive Interactive Computing"
  description: |
    IPython provides a rich architecture for interactive computing with
    a powerful interactive shell, a kernel for Jupyter, high performance
    tools for parallel computing, and more.
  doc_url: https://ipython.readthedocs.io/
  dev_url: https://github.com/ipython/ipython

extra:
  recipe-maintainers:
    - Carreau
    - jakirkham
    - fperez
    - ellisonbg
    - minrk
    - ocefpaf
    - pelson
    - takluyver
    - bollwyvl
    - blink1073

app:
  entry: ipython

...
